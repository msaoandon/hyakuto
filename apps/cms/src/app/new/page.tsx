import { NewGame } from "@/components/NewGame";

// Create-a-game page, at its own URL. The per-game bootstrap: name it, then import
// the current demo or start empty.
export default function NewGamePage() {
  return <NewGame />;
}
